# iOS Messages Generator (PWA)

Mobile-only веб-приложение, полностью имитирующее приложение "Сообщения" (iOS iMessage) в тёмной теме. Предназначено для генерации сообщений с маршрутами и кодами.

## 📱 Особенности

*   **Полная имитация iOS**: Дизайн, шрифты, анимации и поведение идентичны нативному приложению.
*   **PWA (Progressive Web App)**: Можно добавить на главный экран iPhone ("На экран 'Домой'"), и оно будет работать как обычное приложение (без адресной строки браузера).
*   **Автоматические ответы**: При отправке кода автоматически генерируется ответное сообщение с текущей датой, временем и уникальным суффиксом.
*   **Сохранение истории**: Все сообщения и настройки маршрута сохраняются в памяти устройства (localStorage).
*   **Два режима**: можно ввести маршрут и госномер вручную или подтянуть их из Onay API по коду терминала (старый ручной режим сохранён).
*   **Тёмная тема**: Оптимизировано для OLED экранов (iPhone Pro/Pro Max).

## 🚀 Как запустить

### Локальный запуск (для разработки)

1.  Установите зависимости:
    ```bash
    pnpm install
    ```
2.  Поднимите API (порт 3000 по умолчанию):
    ```bash
    pnpm dev:api
    ```
3.  В другом терминале запустите клиент (Vite dev-сервер на 5173, прокси на /api):
    ```bash
    pnpm dev
    ```

### Сборка для продакшена

1.  Соберите проект:
    ```bash
    pnpm build
    ```
2.  Файлы будут в папке `dist`. Их можно загрузить на любой статический хостинг (Vercel, Netlify, GitHub Pages).

## 📲 Как установить на iPhone

1.  Откройте сайт в Safari.
2.  Нажмите кнопку "Поделиться" (квадрат со стрелкой вверх).
3.  Выберите "На экран 'Домой'" (Add to Home Screen).
4.  Нажмите "Добавить".
5.  Запускайте с иконки на рабочем столе.

## 🛠 Технологии

*   React 19
*   Vite
*   Tailwind CSS 4
*   date-fns
*   wouter (Routing)
*   PWA (Manifest + Service Worker)

## 📝 Использование

1.  На главном экране введите **Маршрут** и **Гос. номер** вручную **или** нажмите «Получить из Onay» и укажите код терминала (нужен запущенный API-сервер и креды).
2.  Нажмите "Продолжить".
3.  В чате введите любой код и нажмите стрелку вверх.
4.  Получите автоматический ответ в формате ONAY!

## 🔑 Переменные окружения для Onay API

Создайте `.env` в корне (или экспортируйте переменные) перед запуском `pnpm dev:api` / `pnpm start`:

```
ONAY_APP_TOKEN=<jwt_from_onay_api>        # без префикса Bearer (он добавится автоматически)
ONAY_DEVICE_ID=<device-id-x-ma-d>
ONAY_PHONE_NUMBER=<phone_with_plus>
ONAY_PASSWORD=<password>
ONAY_PUSH_TOKEN=<push_token>

# Необязательно (есть дефолты)
ONAY_BASE_URL=https://nwqsr0rz5earuiy2t8z8.tha.kz
ONAY_OS=2
ONAY_VERSION=3.2.1
ONAY_USER_AGENT=Onay/3.2.1 (kz.onay.Onay; build:6; iOS 26.2.0) Alamofire/5.4.4
ONAY_CITY_ID=1
ONAY_VERBOSE_LOGS=true   # включает подробные логи в консоли сервера
PORT=3000
```

Пароли и токены не отправляются на клиент — всё ходит через бэкенд /api/onay/qr-start.
